<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Demo 用例：通过眼镜 SDK 接收系统服务共享的 NV21；应用不声明相机或麦克风权限。 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <!-- app_name 同时用于桌面名称和页面标题；模型只走 HTTPS，不开放明文流量。 -->
    <application android:allowBackup="false" android:label="@string/app_name" android:icon="@drawable/ic_inspection"
        android:theme="@style/InspectionTheme" android:usesCleartextTraffic="false">
        <activity android:name=".InspectionActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

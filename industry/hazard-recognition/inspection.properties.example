# 用例：复制为 inspection.properties 后填写开发者自己的配置；已有文件时直接编辑，避免覆盖。
# 完整 HTTPS Chat Completions 地址，例如 https://your-gateway.example.com/v1/chat/completions
endpoint=
# 填写开发者实际部署的视觉模型名；接口需支持 image_url 内嵌 Base64 图片。
model=
# 在本机填写开发者自己的 API Key 或网关 Bearer 令牌；此处故意留空，不提交真实密钥。
apiKey=
# 每秒 NV21 本地采样目标，范围 1–3；相似帧会过滤，不代表每秒推理次数。
sampleFps=3
# 修改本机配置后需重新构建安装。配置会进入 APK，带真实密钥的 APK 仅用于私有演示。
# 配置步骤见 README.md「大模型接入与开发者配置」。
